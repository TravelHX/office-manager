{
  "name": "office-manager-e2e",
  "version": "0.0.0-private",
  "private": true,
  "description": "Repo-root package.json for Playwright end-to-end tests. The application itself lives under src/frontend.",
  "scripts": {
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
