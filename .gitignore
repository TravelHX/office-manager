node_modules/
npm-debug.log
.env
.env.local
.env.*.local
coverage/
*.log
.DS_Store
dist/
build/

# Playwright end-to-end test artifacts
test-results/
playwright-report/
.playwright/

# Phase 23d/e: admin-uploaded floor plan images live under data/maps/ at
# runtime. The schema row in floor_plans references the file by path; the
# images themselves are runtime state and are not tracked in source.
data/maps/

# Local configuration files (not tracked in Git)
agents.md
